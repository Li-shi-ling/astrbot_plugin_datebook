{
  "enable_daily_broadcast": {
    "description": "是否启用每日自定义节日播报",
    "type": "bool",
    "default": false,
    "hint": "开启后会在 broadcast_time 查找当天节日，并按节日所属会话分别发送"
  },
  "broadcast_time": {
    "description": "每日播报时间",
    "type": "string",
    "default": "12:00",
    "hint": "24 小时制，格式为 HH:mm，例如 12:00"
  }
}
